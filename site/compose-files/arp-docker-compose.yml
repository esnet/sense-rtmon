version: '3.8'

services:
  arp-exporter:
    image: arp_exporter
    network_mode: "host"
    restart: always
    volumes:
      # do not move this line, auto generated config file matching arp.yml
      - ../Metrics/ARPMetrics/jsonFiles:/jsonFiles
      - ../Metrics/ARPMetrics/arpFiles:/arpFiles
      - ../Metrics/ARPMetrics/pingStat:/pingStat
    ports:
      - 9192:9192
    # deploy:
    #   restart_policy:
    #     condition: on-failure

# please do not change the position of volumes. It's dynamically built line 8 (count from 0)