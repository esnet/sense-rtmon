apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f snmp-docker-compose.yml
    kompose.version: 1.26.1 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: snmp-exporter
  name: snmp-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: snmp-exporter
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f snmp-docker-compose.yml
        kompose.version: 1.26.1 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.service: snmp-exporter
    spec:
      containers:
        - image: prom/snmp-exporter
          name: snmp-exporter
          ports:
            - containerPort: 9116
          resources: {}
          volumeMounts:
            - mountPath: /etc/snmp_exporter/snmp.yml
              name: snmp-exporter-claim0
      restartPolicy: Always
      volumes:
        - name: snmp-exporter-claim0
          persistentVolumeClaim:
            claimName: snmp-exporter-claim0
status: {}
